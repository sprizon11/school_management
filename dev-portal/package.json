{
  "name": "school-dev-portal",
  "private": true,
  "description": "Private platform console — not for app users",
  "scripts": {
    "build": "node scripts/generate-config.js",
    "start": "npm run build && npx --yes serve . -p 5199"
  }
}
